import { PrismaClient } from "@prisma/client";

export  async function listLoad(req, res) {
  try {
    const LoadTypeList = await prisma.loadtype.findMany({
      where: {
        loadTypeActive: 1,
      },
      select: {
        loadTypeName: true,
        
      },
    });

    return res.status(200).json(LoadTypeList, { success: true });
    console.log(accountslist);
    
  } catch (error) {
    console.log(error);
    
    return;
        res.status(350).json({ error: "Error creating account.", success: false });
  }
}